## How to install

**Use Node Version 20**

- Set `.env` like `.env.example`
- Run `yarn install`
- Run `yarn build`
- Run `yarn start`

- If there is an error too many attempts log out then log in again!!

